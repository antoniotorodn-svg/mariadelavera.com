---
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  canonical?: string;
  ogImage?: string;
  schema?: any;
}

const {
  title,
  description,
  canonical = Astro.url.pathname,
  ogImage = '/img/og-image.jpg',
  schema
} = Astro.props;

const siteUrl = 'https://mariadelavera.com';
const fullUrl = `${siteUrl}${canonical}`;
const fullOgImage = `${siteUrl}${ogImage}`;

// Schema por defecto: LocalBusiness
const defaultSchema = {
  "@context": "https://schema.org",
  "@type": ["Dietitian", "LocalBusiness"],
  "name": "María De la Vera - Nutricionista Boadilla del Monte",
  "description": "Dietista-Nutricionista colegiada MAD01182 en Boadilla del Monte. Consulta presencial y online. Pérdida de peso, nutrición deportiva, salud digestiva, hormonal y clínica.",
  "image": fullOgImage,
  "url": siteUrl,
  "telephone": "+34611438679",
  "email": "nutricionista.mariadelavera@gmail.com",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Av. Siglo XXI, 13, local 13/14, Centro Médico Arboleda",
    "addressLocality": "Boadilla del Monte",
    "addressRegion": "Madrid",
    "postalCode": "28660",
    "addressCountry": "ES"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": "40.4021",
    "longitude": "-3.8777"
  },
  "priceRange": "50€ - 140€",
  "paymentAccepted": "Cash, Credit Card, Bizum",
  "openingHoursSpecification": [
    {
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
      "opens": "10:00",
      "closes": "14:00"
    },
    {
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
      "opens": "16:00",
      "closes": "20:00"
    }
  ],
  "sameAs": [
    "https://www.google.es/maps/place//data=!4m2!3m1!1s0xd418ff572a44c05:0x4936e78853ca7fde"
  ]
};

const schemas = schema ? [defaultSchema, schema] : [defaultSchema];
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- SEO Principal -->
  <title>{title}</title>
  <meta name="description" content={description}>
  <link rel="canonical" href={fullUrl}>

  <!-- Open Graph / Facebook -->
  <meta property="og:type" content="website">
  <meta property="og:url" content={fullUrl}>
  <meta property="og:title" content={title}>
  <meta property="og:description" content={description}>
  <meta property="og:image" content={fullOgImage}>
  <meta property="og:locale" content="es_ES">
  <meta property="og:site_name" content="María De la Vera - Nutricionista">

  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:url" content={fullUrl}>
  <meta name="twitter:title" content={title}>
  <meta name="twitter:description" content={description}>
  <meta name="twitter:image" content={fullOgImage}>

  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">

  <!-- Google Fonts: DM Serif Display + DM Sans -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&family=DM+Serif+Display:ital@0;1&display=swap" rel="stylesheet">

  <!-- Schema.org JSON-LD -->
  {schemas.map(s => (
    <script type="application/ld+json" set:html={JSON.stringify(s)} />
  ))}

  <!-- Preconnect para optimización -->
  <link rel="preconnect" href="https://player.vimeo.com">
  <link rel="preconnect" href="https://assets.calendly.com">
  <link rel="dns-prefetch" href="https://vimeo.com">
</head>
<body>
  <slot name="nav" />

  <main>
    <slot />
  </main>

  <slot name="footer" />

  <!-- Mobile Bottom Bar -->
  <div class="mobile-bottom-bar">
    <a href="https://wa.link/8y1ykx" class="btn btn--whatsapp" target="_blank" rel="noopener">
      💬 WhatsApp
    </a>
    <a href="#reservar" class="btn btn--primary">
      📅 Reservar cita
    </a>
  </div>

  <!-- Scroll Reveal Animation -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const revealElements = document.querySelectorAll('.section, .card, .hero, .cta-box, .value-item, .material-card, .testimonial-card, .benefit-item, .pricing-card, .trust-strip__item');

      const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('revealed');
            observer.unobserve(entry.target);
          }
        });
      }, {
        threshold: 0.1,
        rootMargin: '0px 0px -40px 0px'
      });

      revealElements.forEach((el) => {
        el.classList.add('reveal-on-scroll');
        observer.observe(el);
      });
    });
  </script>
</body>
</html>
