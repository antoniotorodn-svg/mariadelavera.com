---
// Nav sticky con blur, logo, links y hamburguesa
---

<nav class="nav">
  <a href="/" class="nav-logo">
    <svg width="180" height="38" viewBox="0 0 180 38" xmlns="http://www.w3.org/2000/svg">
      <text x="0" y="22" font-family="DM Serif Display, Georgia, serif" font-size="20" fill="#2D2D2D" font-weight="400">María De la Vera</text>
      <text x="0" y="35" font-family="DM Sans, sans-serif" font-size="11" fill="#4A8B6F" font-weight="600">Nutricionista</text>
    </svg>
  </a>
  <ul class="nav-links" id="navLinks">
    <li><a href="#proceso">Proceso</a></li>
    <li><a href="#precios">Precios</a></li>
    <li><a href="#reservar">Reservar</a></li>
    <li><a href="#testimonios">Testimonios</a></li>
    <li><a href="#faq">FAQ</a></li>
  </ul>
  <button class="nav-hamburger" onclick="document.getElementById('navLinks').classList.toggle('open')" aria-label="Abrir menú">&#9776;</button>
</nav>

<style>
  .nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 14px 40px;
    background: rgba(253,248,243,0.92);
    position: sticky;
    top: 0;
    z-index: 100;
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    border-bottom: 1px solid rgba(0,0,0,0.04);
  }

  .nav-logo {
    display: block;
    text-decoration: none;
  }

  .nav-links {
    display: flex;
    gap: 32px;
    list-style: none;
  }

  .nav-links a {
    font-size: 0.92em;
    font-weight: 600;
    color: var(--text);
    transition: color 0.2s;
    text-decoration: none;
  }

  .nav-links a:hover {
    color: var(--green-dark);
  }

  .nav-hamburger {
    display: none;
    background: none;
    border: 2px solid var(--sand-dark);
    border-radius: 8px;
    padding: 6px 10px;
    font-size: 1.3em;
    cursor: pointer;
    color: var(--text);
  }

  @media (max-width: 900px) {
    .nav {
      padding: 10px 16px;
    }

    .nav-links {
      display: none;
    }

    .nav-links.open {
      display: flex !important;
      flex-direction: column;
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      background: var(--cream);
      padding: 20px 24px;
      gap: 16px;
      border-bottom: 1px solid var(--sand-dark);
      box-shadow: 0 8px 24px rgba(0,0,0,0.06);
      z-index: 200;
    }

    .nav-hamburger {
      display: block;
    }
  }
</style>
