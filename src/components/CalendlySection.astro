---
import CalendlyEmbed from './CalendlyEmbed.astro';
---

<section class="section" id="reservar">
  <div class="container">
    <div class="calendly-header">
      <h2 class="section-title">Reserva tu cita</h2>
      <p class="section-sub">
        Elige la modalidad que prefieras y selecciona el día y hora que mejor te convenga.
      </p>
    </div>

    <div class="calendly-grid">
      <div class="calendly-col">
        <div class="calendly-col__label">
          <span class="calendly-col__icon">&#128205;</span>
          Presencial
        </div>
        <CalendlyEmbed type="presencial" />
      </div>

      <div class="calendly-col">
        <div class="calendly-col__label">
          <span class="calendly-col__icon">&#128187;</span>
          Online
        </div>
        <CalendlyEmbed type="online" />
      </div>
    </div>

    <p class="text-center" style="margin-top: var(--spacing-lg); color: var(--text-light);">
      ¿Vienen en pareja? <a href="https://calendly.com/nutricionista-mariadelavera/primera-consulta-parejas" target="_blank" rel="noopener" style="color: var(--green-dark); font-weight: 600;">Reserva consulta para parejas aquí</a>
    </p>
  </div>
</section>

<style>
  .calendly-header {
    margin-bottom: var(--spacing-xl);
  }

  .calendly-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
  }

  @media (max-width: 900px) {
    .calendly-grid {
      grid-template-columns: 1fr;
      gap: 32px;
    }
  }

  .calendly-col__label {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.375rem;
    padding: 0.625rem 1.25rem;
    background: var(--sand);
    border-radius: var(--radius-full);
    font-family: var(--font-body);
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--text);
    max-width: 180px;
    margin: 0 auto;
  }

  .calendly-col__icon {
    font-size: 1rem;
  }
</style>
