---
interface TrustItem {
  icon: string;
  text: string;
}

interface Props {
  items?: TrustItem[];
}

const {
  items = [
    { icon: "✓", text: "Colegiada oficial MAD01182" },
    { icon: "✓", text: "CEU San Pablo + UDIMA" },
    { icon: "✓", text: "+700 pacientes atendidos" },
    { icon: "✓", text: "Presencial + Online" }
  ]
} = Astro.props;
---

<div class="trust-strip">
  <div class="container">
    <div class="trust-strip__grid">
      {items.map(item => (
        <div class="trust-strip__item">
          <span class="trust-strip__icon">{item.icon}</span>
          <span class="trust-strip__text">{item.text}</span>
        </div>
      ))}
    </div>
  </div>
</div>

<style>
  .trust-strip {
    padding: var(--spacing-md) 0;
    background-color: var(--mentaris-green-light);
    border-top: 1px solid rgba(124, 182, 157, 0.15);
    border-bottom: 1px solid rgba(124, 182, 157, 0.15);
  }

  .trust-strip__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-sm);
    text-align: center;
  }

  @media (min-width: 640px) {
    .trust-strip__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .trust-strip__grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .trust-strip__item {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  .trust-strip__icon {
    color: var(--mentaris-green-dark);
    font-weight: 700;
    font-size: 0.875rem;
  }

  .trust-strip__text {
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--mentaris-green-dark);
    font-family: var(--font-body);
  }
</style>
