---
// Propuesta de valor - 4 bloques
---

<div class="value-grid">
  <div class="value-item">
    <div class="value-icon-wrap">
      <span class="value-emoji">🍏</span>
    </div>
    <h3 class="value-title">Plan 100% personalizado</h3>
    <p class="value-text">
      Cada cuerpo es único, por eso tu plan está adaptado a tus objetivos, gustos y estilo de vida.
    </p>
  </div>

  <div class="value-item">
    <div class="value-icon-wrap">
      <span class="value-emoji">🎯</span>
    </div>
    <h3 class="value-title">Acompañamiento continuo</h3>
    <p class="value-text">
      Resolución de dudas y ajustes constantes para que siempre estés acompañado en el proceso.
    </p>
  </div>

  <div class="value-item">
    <div class="value-icon-wrap">
      <span class="value-emoji">📊</span>
    </div>
    <h3 class="value-title">Enfoque basado en ciencia</h3>
    <p class="value-text">
      Sin dietas milagro. Todo está respaldado por evidencia científica y experiencia clínica.
    </p>
  </div>

  <div class="value-item">
    <div class="value-icon-wrap">
      <span class="value-emoji">🥗</span>
    </div>
    <h3 class="value-title">Sin restricciones absurdas</h3>
    <p class="value-text">
      No se trata de prohibir, sino de crear un equilibrio sostenible que puedas mantener siempre.
    </p>
  </div>
</div>

<style>
  .value-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-lg);
    max-width: 1000px;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .value-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .value-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .value-item {
    text-align: center;
    padding: var(--spacing-lg) var(--spacing-md);
    background: var(--color-white);
    border-radius: var(--radius-md);
    border: 1px solid var(--mentaris-border);
    transition: all var(--transition-base);
  }

  .value-item:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
    border-color: rgba(124, 182, 157, 0.3);
  }

  .value-icon-wrap {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    background: var(--mentaris-green-light);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto var(--spacing-md);
    transition: all var(--transition-base);
  }

  .value-item:hover .value-icon-wrap {
    background: linear-gradient(135deg, var(--mentaris-green-light) 0%, var(--mentaris-cream) 100%);
    transform: scale(1.08);
  }

  .value-emoji {
    font-size: 1.5rem;
  }

  .value-title {
    font-family: var(--font-display);
    font-size: 1.1rem;
    font-weight: 400;
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-sm);
    line-height: 1.3;
  }

  .value-text {
    font-size: 0.9rem;
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin: 0;
  }
</style>
